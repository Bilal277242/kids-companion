{
  "name": "@kids/voice",
  "version": "0.0.0",
  "private": true,
  "description": "Speech-to-text and text-to-speech ports, adapters, and audio normalisation.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo",
    "test": "vitest run --project unit --passWithNoTests"
  },
  "dependencies": {
    "@kids/types": "workspace:*"
  }
}
